b_obstacle_detector:
ros_parameters:

hz: 10                   # ループ周期 [Hz]
laser_step: 1          # 何本ずつレーザを見るか(全1080本)
robot_frame: "base_link" # frame_id

# --- はずれ値対策 ---
ignore_dist: 0.1         # はずれ値とみなす距離 [m]